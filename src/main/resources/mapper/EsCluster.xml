<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- 为这个mapper指定一个唯一的namespace，namespace的值习惯上设置成包名+sql映射文件名，这样就能够保证namespace的值是唯一的
例如namespace="me.gacl.mapping.userMapper"就是me.gacl.mapping(包名)+userMapper(userMapper.xml文件去除后缀)
 -->
<mapper namespace="com.fosun.fin.data.dao.EsClusterDao">
<!--
    <select id="getUserByName" parameterType="String"
            resultType="TElasticSearchCluster">
        select * from user where name=#{name}
    </select>

    <insert id="addCluster" useGeneratedKeys="true" keyProperty="id" parameterType="TElasticSearchCluster">
        insert into user (id,name,age) values (#{user.id},#{user.name},#{user.age})
    </insert>


    <select id="selectById" parameterType="int" resultType="User">
        select * from t_user where id = #{id}
    </select>

    <select id="selectByUsername" parameterType="string" resultType="User">
        select * from t_user where username = #{username}
    </select>

    <update id="update" parameterType="User">
        update t_user
        <set>
            <if test="password!=null">password=#{password},</if>
            <if test="email!=null">email=#{email}</if>
        </set>
        where id = #{id}
    </update>

    <delete id="deleteById" parameterType="int">
        delete from
        t_user
        where id
        = #{id}
    </delete>-->

</mapper>